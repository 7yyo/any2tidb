# any2tidb — Any DB to TiDB
#
# Connection info only. All runtime parameters are passed via CLI flags.
# Passwords use environment variables: SOURCE_PASSWORD, TARGET_PASSWORD

source:
  host: 127.0.0.1
  port: 1433
  username: sa
  password: ${SOURCE_PASSWORD:test@123}

target:
  host: 127.0.0.1
  port: 4000
  username: root
  password: ${TARGET_PASSWORD:}
